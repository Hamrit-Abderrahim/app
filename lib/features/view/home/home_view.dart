import 'package:flutter/material.dart';

class HomeVew extends StatelessWidget {
  const HomeVew({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('HElllllllllllllllo'),
      ),
    );
  }
}
